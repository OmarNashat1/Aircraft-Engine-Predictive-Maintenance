import 'package:flutter/material.dart';

/// Application color palette - Light Theme
class AppColors {
  // Backgrounds
  static const backgroundPrimary = Color(0xFFF5F7FA); // Light gray background
  static const backgroundSurface = Color(0xFFFFFFFF); // White cards
  static const backgroundElevated = Color(0xFFFAFBFC); // Slightly elevated

  // Primary/Accent
  static const accentBlue = Color(0xFF1E6FD9); // Primary blue
  static const accentBlueLight = Color(0xFFE8F2FF); // Light blue background

  // Status Colors
  static const statusOk = Color(0xFF2EA043); // Green
  static const statusWatch = Color(0xFFD29922); // Amber/Yellow
  static const statusCritical = Color(0xFFDA3633); // Red

  // Text Colors
  static const textPrimary = Color(0xFF1F2937); // Dark gray
  static const textSecondary = Color(0xFF6B7280); // Medium gray
  static const textTertiary = Color(0xFF9CA3AF); // Light gray

  // Border Colors
  static const borderLight = Color(0xFFE5E7EB);
  static const borderMedium = Color(0xFFD1D5DB);

  // Sidebar
  static const sidebarBackground = Color(0xFF1F2937); // Dark sidebar
  static const sidebarActiveItem = Color(0xFF1E6FD9);
  static const sidebarText = Color(0xFFFFFFFF);
  static const sidebarTextInactive = Color(0xFF9CA3AF);
}
